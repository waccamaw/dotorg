# Waccamaw.org Visual Architecture

This file contains ASCII diagrams and visual explanations of the system architecture.

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         waccamaw.org                                 │
│                    (Cloudflare DNS & Proxy)                         │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           │ All traffic flows through Cloudflare
                           │
                ┌──────────▼──────────┐
                │  Cloudflare Worker  │
                │   (Edge Router)     │
                │                     │
                │  Path-based routing │
                └──────────┬──────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        │                  │                  │
    ┌───▼────┐       ┌─────▼─────┐      ┌────▼──────┐
    │  /home/ │       │ /updates/ │      │ /members/ │
    │         │       │           │      │           │
    │ Framer  │       │Micro.blog │      │ Frontend  │
    │         │       │   (Hugo)  │      │ + Backend │
    └─────────┘       └───────────┘      └─────┬─────┘
     External          This Repo           This Repo
     Platform          GitHub Sync         + API Service
                                                │
                                         ┌──────▼──────┐
                                         │ Backend API │
                                         │ (member-    │
                                         │  services)  │
                                         └─────────────┘
```
```

## Request Flow - User Visits waccamaw.org/updates/

```
┌─────────┐
│ Browser │
└────┬────┘
     │ 1. HTTPS request: waccamaw.org/updates/some-post
     │
┌────▼──────────────────┐
│ Cloudflare Edge (POP) │
│   DNS Resolution      │
└────┬──────────────────┘
     │ 2. Route to Cloudflare Worker
     │
┌────▼──────────────────┐
│  Cloudflare Worker    │
│                       │
│  if (path.startsWith  │
│      ('/updates/'))   │
│    → Micro.blog       │
└────┬──────────────────┘
     │ 3. Fetch from origin
     │
┌────▼──────────────────┐
│  waccamaw.micro.blog  │
│   Static HTML (Hugo)  │
└────┬──────────────────┘
     │ 4. Return response
     │
┌────▼──────────────────┐
│ Cloudflare Edge Cache │
│   Cache for 1 hour    │
└────┬──────────────────┘
     │ 5. Deliver to user
     │
┌────▼────┐
│ Browser │
└─────────┘
```

## Content Update Flow - Developer Makes Changes

```
┌──────────────┐
│  Developer   │
│   (VS Code)  │
└──────┬───────┘
       │ 1. Edit content/2025/11/18/post.md
       │    git commit & push
┌──────▼────────┐
│ GitHub Repo   │
│ waccamaw/     │
│ dotorg        │
└──────┬────────┘
       │ 2. Webhook trigger
       │
┌──────▼──────────┐
│  Micro.blog     │
│   Platform      │
│                 │
│  - Pull from    │
│    GitHub       │
│  - Run Hugo     │
│    build        │
│  - Generate     │
│    static HTML  │
└──────┬──────────┘
       │ 3. Deploy to CDN
       │
┌──────▼──────────┐
│  Micro.blog CDN │
│  waccamaw       │
│  .micro.blog    │
└──────┬──────────┘
       │ 4. Proxied via Cloudflare Workers
       │
┌──────▼──────────────┐
│  waccamaw.org       │
│  /updates/          │
│                     │
│  ✅ Live!           │
└─────────────────────┘
```

## Multi-Platform Integration

```
                    waccamaw.org
                         │
        ┌────────────────┼────────────────┐
        │                │                │
    /home/*         /updates/*       /members/*
        │                │                │
        │                │                │
┌───────▼────────┐ ┌─────▼──────┐ ┌───────▼────────┐
│                │ │            │ │                │
│  Framer Site   │ │ Micro.blog │ │  Future Portal │
│                │ │            │ │                │
│ ┌────────────┐ │ │ ┌────────┐ │ │ ┌────────────┐ │
│ │  About     │ │ │ │ Blog   │ │ │ │ Login      │ │
│ │  History   │ │ │ │ News   │ │ │ │ Directory  │ │
│ │  Events    │ │ │ │ Meetings│ │ │ │ Documents  │ │
│ │  Contact   │ │ │ │ Photos │ │ │ │ Voting     │ │
│ └────────────┘ │ │ └────────┘ │ │ └────────────┘ │
│                │ │            │ │                │
│  Marketing &   │ │ Public     │ │ Private        │
│  Public Info   │ │ Content    │ │ Member Area    │
│                │ │            │ │                │
│  Managed via   │ │ Git-based  │ │ Authentication │
│  Framer UI     │ │ (GitHub)   │ │ Required       │
└────────────────┘ └────────────┘ └────────────────┘
     External         This Repo       Planned
```

## GitHub Copilot Workflow

```
┌──────────────────────────────────────────────────────────────┐
│                    Contributor Journey                        │
└──────────────────────────────────────────────────────────────┘

Step 1: Create Issue
┌────────────────┐
│ GitHub Issues  │
│                │
│ Choose:        │
│ • Bug Report   │
│ • Feature      │
│ • Content      │
│ • Docs         │
└────────┬───────┘
         │
         ▼
Step 2: Ask Copilot
┌────────────────────────┐
│ VS Code + Copilot      │
│                        │
│ "How do I add a blog   │
│  post about the        │
│  November meeting?"    │
│                        │
│ Copilot suggests:      │
│ 1. Create file path    │
│ 2. Add front matter    │
│ 3. Write content       │
└────────┬───────────────┘
         │
         ▼
Step 3: Make Changes
┌────────────────────┐
│ Local Development  │
│                    │
│ $ just serve       │
│                    │
│ Test at:           │
│ localhost:1313     │
└────────┬───────────┘
         │
         ▼
Step 4: Take Screenshots
┌──────────────────────┐
│ Desktop (1920x1080)  │
│ screenshot.png       │
│                      │
│ Mobile (390x844)     │
│ mobile.png           │
└────────┬─────────────┘
         │
         ▼
Step 5: Create PR
┌────────────────────────┐
│ Pull Request Template  │
│                        │
│ ✅ Description         │
│ ✅ Desktop screenshot  │
│ ✅ Mobile screenshot   │
│ ✅ Testing checklist   │
│ ✅ Link to issue       │
└────────┬───────────────┘
         │
         ▼
Step 6: Review & Merge
┌────────────────────┐
│ Review Process     │
│                    │
│ Maintainer reviews │
│ Copilot may        │
│ suggest fixes      │
│                    │
│ Approve & Merge    │
└────────┬───────────┘
         │
         ▼
Step 7: Auto-Deploy
┌────────────────────┐
│ Deployment         │
│                    │
│ GitHub → Micro.blog│
│ Hugo build         │
│ Deploy to CDN      │
│ Live in ~5 min     │
└────────────────────┘
```

## Repository Structure Visual

```
dotorg/
│
├── .github/                    ← GitHub Configuration
│   ├── copilot-instructions.md    🤖 Copilot agent guidelines
│   ├── pull_request_template.md   📋 PR checklist
│   └── ISSUE_TEMPLATE/             📝 Issue forms
│       ├── bug_report.md
│       ├── feature_request.md
│       ├── content_update.md
│       └── documentation.md
│
├── content/                    ← 📄 All Content (Markdown)
│   ├── about.md                   About the tribe
│   ├── meetings.md                Meeting info
│   ├── photos.md                  Photo gallery
│   ├── style-guide.md             Style guide
│   └── YYYY/MM/DD/                Date-organized posts
│       └── post-name.md           Individual posts
│
├── layouts/                    ← 🎨 Hugo Templates
│   ├── _default/                  Default layouts
│   │   ├── baseof.html           Base template
│   │   ├── single.html           Single post
│   │   ├── list.html             List pages
│   │   └── *.html                Other layouts
│   ├── partials/                  Reusable components
│   │   ├── header.html           Header
│   │   ├── footer.html           Footer
│   │   ├── head.html             HTML <head>
│   │   └── *.html                Other partials
│   └── *.xml/html                 Feed templates
│
├── static/                     ← 🖼️ Static Assets
│   ├── theme.css                  Main stylesheet
│   ├── uploads/                   User uploads
│   └── logos/                     Tribal logos
│       └── doug-2025/             Current logo set
│
├── public/                     ← 🏗️ Generated Site
│   └── [Not committed]            Hugo build output
│
├── config.json                 ⚙️ Hugo Configuration
├── Justfile                    🔧 Task Automation
│
├── README.md                   📖 Project Overview
├── CONTRIBUTING.md             🤝 Contribution Guide
├── ARCHITECTURE.md             🏛️ System Architecture
├── DEPLOYMENT.md               🚀 Deployment Guide
├── DOCS.md                     📚 Documentation Index
├── DIAGRAMS.md                 📊 This File
│
└── LICENSE                     ⚖️ License Info
```

## Deployment Pipeline Visual

```
┌─────────────────────────────────────────────────────────────────┐
│                     Deployment Pipeline                          │
└─────────────────────────────────────────────────────────────────┘

Development
┌──────────────┐
│ Local Branch │
│ feature/     │
│ fix/         │
│ content/     │
└──────┬───────┘
       │ Test locally
       │ $ just serve
       │
       ▼
┌──────────────┐
│ Commit       │
│ git commit   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Push to      │
│ GitHub       │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Pull Request │
│ + Screenshots│
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Code Review  │
│ By maintainer│
└──────┬───────┘
       │ Approved
       ▼
       
Production
┌──────────────┐
│ Merge to     │
│ main         │
└──────┬───────┘
       │ Webhook
       ▼
┌──────────────┐
│ Micro.blog   │
│ - Pull repo  │
│ - Run Hugo   │
│ - Build site │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Deploy to    │
│ CDN          │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Cloudflare   │
│ Proxy        │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ ✅ LIVE      │
│ waccamaw.org │
│ /updates/    │
└──────────────┘

Time: ~5 minutes
```

## Data Flow - Different Content Types

```
┌────────────────────────────────────────────────────────────────┐
│                    Content Type Flows                           │
└────────────────────────────────────────────────────────────────┘

Blog Post:
content/2025/11/18/meeting.md → Hugo → /updates/2025/11/18/meeting/
                                        (HTML page)

Tribal Info:
content/about.md → Hugo → /updates/about/
                          (Static page)

Photo Gallery:
content/photos.md + static/uploads/*.jpg → Hugo → /updates/photos/
                                                   (Grid layout)

RSS Feed:
All posts → Hugo → /updates/feed.xml
                   (RSS feed)

JSON Feed:
All posts → Hugo → /updates/feed.json
                   (JSON feed)

Archive:
All posts → Hugo → /updates/archive/
                   (Date-organized list)
```

## Security Layers

```
┌────────────────────────────────────────────────────────────────┐
│                      Security Stack                             │
└────────────────────────────────────────────────────────────────┘

Layer 1: Cloudflare Edge
┌──────────────────────┐
│ DDoS Protection      │
│ Bot Detection        │
│ Rate Limiting        │
│ WAF Rules            │
└──────┬───────────────┘
       │
Layer 2: SSL/TLS
┌──────▼───────────────┐
│ Full (Strict) Mode   │
│ TLS 1.3              │
│ HSTS Enabled         │
│ Auto Certificates    │
└──────┬───────────────┘
       │
Layer 3: Cloudflare Workers
┌──────▼───────────────┐
│ Routing Logic        │
│ Request Validation   │
│ Header Security      │
└──────┬───────────────┘
       │
Layer 4: Origin (Micro.blog)
┌──────▼───────────────┐
│ Static HTML Only     │
│ No User Input        │
│ Read-Only CDN        │
└──────┬───────────────┘
       │
Layer 5: Source Control
┌──────▼───────────────┐
│ GitHub 2FA           │
│ Branch Protection    │
│ Required Reviews     │
│ Signed Commits (opt) │
└──────────────────────┘
```

## Mobile-First Responsive Design

```
┌────────────────────────────────────────────────────────────────┐
│                  Responsive Breakpoints                         │
└────────────────────────────────────────────────────────────────┘

📱 Mobile (Default)
┌──────────┐
│  < 768px │
│          │
│ Single   │
│ Column   │
│          │
│ Stack    │
│ Elements │
└──────────┘
Base styles
Mobile-first

     ↓
     
📱 Tablet
┌────────────────┐
│   768px - 1023px│
│                 │
│  Two Column     │
│  Some spacing   │
└────────────────┘
@media (min-width: 768px)

     ↓
     
💻 Desktop
┌──────────────────────────┐
│      ≥ 1024px            │
│                          │
│  Full Layout             │
│  Max width container     │
│  Optimal spacing         │
└──────────────────────────┘
@media (min-width: 1024px)

Test Sizes:
• 390x844  (iPhone 12 Pro)
• 820x1180 (iPad)
• 1920x1080 (Desktop)
```

## Member Portal Architecture

### Frontend + Backend Separation

```
┌─────────────────────────────────────────────────────────────────┐
│                     Member Portal (/members/)                    │
└────────────────────────┬────────────────────────────────────────┘
                         │
        ┌────────────────┴────────────────┐
        │                                 │
        ▼                                 ▼
┌────────────────┐              ┌──────────────────┐
│    Frontend    │              │     Backend      │
│   (This Repo)  │◄────────────►│ (member-services)│
│                │   REST API    │                  │
│  Static Files  │   JWT Auth    │  Node.js/Express │
│  HTML/CSS/JS   │               │  + Database      │
└────────────────┘              └──────────────────┘
       │                                 │
       │ Deploy to:                      │ Deploy to:
       ▼                                 ▼
┌────────────────┐              ┌──────────────────┐
│ Cloudflare     │              │  Railway/Fly.io  │
│ Pages          │              │  Render/Heroku   │
│ (CDN)          │              │                  │
└────────────────┘              └──────────────────┘

User → Cloudflare Workers → Frontend (CDN)
                          ↓
                     API Requests → Backend → Database
```

### Member Portal Request Flow

```
┌─────────┐
│ User    │
│ Browser │
└────┬────┘
     │ 1. Visit waccamaw.org/members/
     │
┌────▼──────────────────┐
│ Cloudflare Worker     │
│ Routes /members/* to  │
│ Static Frontend       │
└────┬──────────────────┘
     │ 2. Serve index.html + assets
     │
┌────▼──────────────────┐
│  Frontend App Loads   │
│  - config.js          │
│  - api.js             │
│  - auth.js            │
│  - app.js             │
└────┬──────────────────┘
     │ 3. User logs in
     │
┌────▼──────────────────┐
│  POST /api/auth/login │
│  email + password     │
└────┬──────────────────┘
     │ 4. API Request
     │
┌────▼──────────────────────┐
│  Backend API              │
│  member-services          │
│                           │
│  1. Validate credentials  │
│  2. Generate JWT token    │
│  3. Return user + token   │
└────┬──────────────────────┘
     │ 5. Response
     │
┌────▼──────────────────┐
│  Frontend saves:      │
│  - Token → localStorage│
│  - User data          │
│  - Show dashboard     │
└────┬──────────────────┘
     │ 6. Load dashboard data
     │
┌────▼──────────────────────┐
│  Parallel API Requests    │
│  (with Bearer token)      │
│                           │
│  GET /api/documents       │
│  GET /api/events          │
│  GET /api/announcements   │
└────┬──────────────────────┘
     │ 7. Responses
     │
┌────▼──────────────────┐
│  Render Dashboard     │
│  - Stats              │
│  - Document list      │
│  - Event list         │
│  - Announcements      │
└───────────────────────┘
```

### Member Portal File Structure

```
members/
├── index.html                 Main HTML page
│   ├── Login Screen          #loginScreen
│   ├── Register Screen       #registerScreen
│   └── Dashboard             #memberDashboard
│
├── assets/
│   ├── css/
│   │   └── styles.css        All styles, mobile-first
│   │       ├── Variables     :root colors/spacing
│   │       ├── Base styles   Typography, layout
│   │       ├── Components    Cards, forms, buttons
│   │       ├── Auth screens  Login/register
│   │       ├── Dashboard     Stats, lists, actions
│   │       └── Responsive    Media queries
│   │
│   └── js/
│       ├── config.js         ⚙️  Configuration
│       │   ├── API_BASE_URL
│       │   ├── Endpoints
│       │   └── Settings
│       │
│       ├── api.js            🔌 API Client
│       │   ├── request()     HTTP wrapper
│       │   ├── get/post/put/delete
│       │   ├── login()
│       │   ├── register()
│       │   ├── getDocuments()
│       │   └── getEvents()
│       │
│       ├── auth.js           🔐 Auth Manager
│       │   ├── init()        Check token
│       │   ├── login()       Handle login
│       │   ├── logout()      Clear session
│       │   └── getCurrentUser()
│       │
│       └── app.js            🎯 Main App
│           ├── init()
│           ├── setupEventListeners()
│           ├── handleLogin()
│           ├── handleRegister()
│           ├── showDashboard()
│           └── loadDashboardData()
│
├── .env.example              Environment template
├── .gitignore               Git ignore rules
├── QUICKSTART.md            Quick start guide
└── README.md                Full documentation
```

### Authentication Flow Details

```
┌──────────────┐
│ User enters  │
│ credentials  │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│ app.handleLogin()    │
│ - Get form values    │
│ - Show loading       │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ auth.login()         │
│ - Call API           │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ api.login()          │
│ - POST /auth/login   │
│ - Save token         │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Backend validates    │
│ - Check email/pass   │
│ - Generate JWT       │
│ - Return user+token  │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Token stored in      │
│ localStorage:        │
│                      │
│ waccamaw_auth_token  │
│ waccamaw_user_data   │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ All API requests     │
│ include header:      │
│                      │
│ Authorization:       │
│ Bearer <token>       │
└──────────────────────┘
```

### Data Flow Pattern

```
User Action → Frontend Handler → API Client → Backend API
                                                    ↓
                                               Database
                                                    ↓
                                             Response
                                                    ↓
Backend API → API Client → Frontend Handler → Update UI
```

**Example: Loading Documents**

```javascript
// User lands on dashboard
app.showDashboard()
    ↓
app.loadDashboardData()
    ↓
api.getDocuments()
    ↓
GET /api/documents
Authorization: Bearer eyJhbG...
    ↓
Backend queries database
    ↓
Returns JSON: { data: [...], total: 5 }
    ↓
app.renderDocuments(data)
    ↓
UI updates with document list
```

---

**Last Updated**: November 20, 2025

**Purpose**: Visual reference for understanding system architecture

**Note**: These are ASCII diagrams. For presentation-quality diagrams, consider tools like:
- Draw.io
- Mermaid (can be embedded in markdown)
- Excalidraw
- PlantUML
